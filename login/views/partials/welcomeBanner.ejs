Total number of commutes is:
<% if(athlete) { %>
    <%= athlete.commutes.length %>
    <% if(athlete.commuteCosts.length >= 1) { %>
	
        Your total Mile Money balance is: 
        <%= mileMoneyBalance - totalRedeemed %>
    <% } else { return ;} %>
    <p>
        Total redeemed Mile Money: <%= totalRedeemed %>

    </p>

<% } %>