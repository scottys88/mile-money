Total number of commutes is:
<% if(athlete) { %>
    <%= athlete.commutes.length %>
    <% if(athlete.commuteCosts.length >= 1) { %>	
        Total saved from commuting is: 
        <%= parseFloat(athlete.commuteCosts[0].totalCost) * parseFloat(athlete.commutes.length) %>
    <% } else { return ;} %>
<% } %>