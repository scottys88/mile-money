Mile Money app.

To start the server, Bash on the myapp directory with the command:

set DEBUG=myapp:* & npm start

This command is from the Express documentation: https://expressjs.com/en/starter/generator.html

To restart the server open cmd ad administrator and run: taskkill /im node.exe /F